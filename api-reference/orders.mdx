---
title: Orders
description: Order listing, creation and cancellation endpoint reference
---

Reads are consistent with your own writes: a create or cancel this API has acknowledged is visible on your next `GET /orders`, `GET /orders/paginated` or `GET /orders/{id}`, so you do not need to poll until it appears or sleep between writing and reading. Those reads wait for the read model to catch up, bounded at 1 second by default. If that bound expires the request is still answered, with what has been written so far, which may be missing your most recent write; re-read rather than re-sending the write. `GET /orders/events` reads a different table and is outside this guarantee.

## List Orders

```text
GET /api/v1/orders
```

**Authentication:** `API key HMAC`

Query parameters:

| Query | Type | Required | Description |
| --- | --- | --- | --- |
| `market` | UUID string | No | Market UUID. |
| `status` | enum string | No | `PENDING`, `EXECUTING`, `EXECUTED` or `REJECTED`. `CANCELLED` is rejected; use `/orders/paginated`. |
| `maker` | address string | No | Maker address. |
| `marketName` | string | No | Case-insensitive partial market name. |
| `sortBy` | enum string | No | `created_at`, `total`, `amount` or `filled`. Default `created_at`. |
| `order` | enum string | No | `ASC` or `DESC`. Default `DESC`. |

Response type: `OrderWithMarket[]`.

When `status` is omitted, the endpoint returns all non-cancelled statuses. Passing `status=CANCELLED` returns `400`.

## List Orders Paginated

```text
GET /api/v1/orders/paginated
```

**Authentication:** `API key HMAC`

Query parameters:

| Query | Type | Required | Description |
| --- | --- | --- | --- |
| `market` | UUID string | No | Market UUID. |
| `status` | enum string | No | `PENDING`, `EXECUTING`, `EXECUTED`, `CANCELLED` or `REJECTED`. |
| `maker` | address string | No | Maker address. |
| `marketName` | string | No | Case-insensitive partial market name. |
| `page` | integer | No | Page number. Default `1`. |
| `limit` | integer | No | Results per page. Default `30`. |
| `sortBy` | enum string | No | `created_at`, `total`, `amount` or `filled`. Default `created_at`. |
| `order` | enum string | No | `ASC` or `DESC`. Default `DESC`. |
| `total` | boolean | No | Include a bounded total count. Default `false`. |
| `cursor` | string | No | Opaque cursor from the previous response. Valid only with the default `created_at` sort. |

When `status` is omitted, this endpoint returns active orders only: `PENDING` and `EXECUTING`.

Response type: `OrderPagination`.

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `first` | integer | Yes | One-based index of the first row. |
| `last` | integer | Yes | One-based index of the last row. |
| `limit` | integer | Yes | Results per page. |
| `total` | integer or null | Yes | Bounded total when `total=true`; otherwise `null`. |
| `totalCapped` | boolean | No | Whether `total` reached the server-side cap. |
| `data` | `OrderWithMarket[]` | Yes | Orders on this page. |
| `hasMore` | boolean | Yes | Whether another cursor page exists. |
| `nextCursor` | string or null | Yes | Opaque cursor for the next page. |

## Get Order

```text
GET /api/v1/orders/{id}
```

**Authentication:** `API key HMAC`

Path parameters:

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | UUID string | Yes | Order UUID. |

Response type: `OrderWithMarket`.

## Create Order

```text
POST /api/v1/orders
```

**Authentication:** `API key HMAC`

Request body type: `CreateOrder`.

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `salt` | string | Yes | Unique uint256 numeric string. Max 78 characters. |
| `taker` | address string | Yes | Taker address. Use the zero address for open GTC maker orders. |
| `tokenId` | string | Yes | Outcome token ID as a numeric string. Max 78 characters. |
| `makerAmount` | string | Yes | Positive integer string in base units. Max 78 characters. |
| `takerAmount` | string | Yes | Positive integer string in base units. Max 78 characters. |
| `expiration` | integer | Yes | Unix timestamp in seconds, or `0`. |
| `feeRateBps` | integer | No | Fee rate in basis points. Range `0` to `10000`. |
| `side` | integer | Yes | `0` buy, `1` sell. |
| `signature` | string | No | Client-side EIP-712 order signature. |
| `orderType` | enum string | Yes | `GTC`, `FOK` or `FAK`. |
| `postOnly` | boolean | No | Maker-only flag. Valid for GTC only; a crossing post-only order is rejected. Default `false`. |
| `priceUint` | string | No | Server-compatible price integer. |
| `idempotencyKey` | UUID v4 string | No | Client key for deduplicating repeated submission of the same logical order. |

Response type: `Order`.

This endpoint returns `503` when the order could not be signed, submitted or admitted. Every `503` here means nothing was created, so re-sending the identical body cannot duplicate the order. See [Admission Errors](#admission-errors). An insufficient balance is a `400`, not a `503`, and retrying it will not help.

## Create Batch Orders

```text
POST /api/v1/orders/batch
```

**Authentication:** `API key HMAC`

Request body:

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `orders` | `CreateOrder[]` | Yes | Orders to create. Minimum `1`, maximum `50`. |

Response type: `CreateBatchOrdersResponse`.

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `results` | `BatchOrderResult[]` | Yes | Per-order results in request order. |

`BatchOrderResult`:

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `index` | integer | Yes | Index of the order in the submitted batch. |
| `success` | boolean | Yes | Whether that order was created. |
| `orderId` | UUID string | No | Created order UUID when successful. |
| `hash` | string | No | EIP-712 hash of the created order when `success` is true, lowercase `0x`-prefixed. The same value the order's WebSocket events and `GET /orders` carry. If you signed the order yourself, compare it with the order you submitted at `index`. |
| `code` | string | No | Machine-readable reason when unsuccessful. |
| `error` | string | No | Error message when unsuccessful. |

The six `errorCode` values listed under [Admission Errors](#admission-errors) arrive on this route as an entry's `code` inside a normal `201`, with no `Retry-After` header, because other entries in the same request may have been created. The `code` set is open-ended: it is whatever the venue supplies for that entry, plus local fallbacks including `bad-request`, `internal-error`, `not-attempted`, `nonce-stale` and `order-archived`. Do not assume the set is closed; treat an unrecognised `code` the way you treat `internal-error`.

This route can still answer a request-level `503` with `error: "SIGNING_UNAVAILABLE"`, in which case no order in the batch was created.

## Cancel Order

```text
POST /api/v1/orders/{id}/cancel
```

**Authentication:** `API key HMAC`

Path parameters:

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | UUID string | Yes | Order UUID. |

Response type: `Order`.

Returns `503` with `error: "ORDER_ADMISSION_UNAVAILABLE"` and `errorCode: "engine-busy"` when the venue declined to admit the cancellation. Nothing was cancelled and the identical request can be re-sent. See [Admission Errors](#admission-errors).

## Cancel Batch Orders

```text
POST /api/v1/orders/batch-cancel
```

**Authentication:** `API key HMAC`

Request body:

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `orderIds` | UUID v4 string[] | Yes | Version-4 order UUIDs to cancel. Minimum `1`, maximum `50`. |

Response type: `BatchCancelOrdersResponse`.

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `results` | `BatchCancelOrderResult[]` | Yes | Per-order cancellation results. |

`BatchCancelOrderResult`:

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `orderId` | UUID string | Yes | Order UUID. |
| `success` | boolean | Yes | Whether the order was cancelled. |
| `error` | string | No | Error message when unsuccessful. |
| `code` | string | No | Machine-readable failure class when unsuccessful. Branch on this rather than matching `error`. `not-attempted` means the batch stopped before reaching this order, so it was not touched and re-submitting its cancel is safe. |

A failed reply is returned as its own status rather than collapsed into a `409`:

| Status | Meaning |
| --- | --- |
| `500` | The venue answered with an error. |
| `408` | No reply within 30 seconds. |
| `503` | The message broker failed. |
| `409` | Another cancel-shaped batch is already in flight. |

None of these carries a `Retry-After` header or an `errorCode`. `408` and `503` are lost-response cases: the orders may or may not have been cancelled, so re-read them before re-issuing.

## Cancel Market Orders

```text
POST /api/v1/orders/cancel
```

**Authentication:** `API key HMAC`

Request body:

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `market` | UUID string | Yes | Market UUID. |

Response type: `Order[]`.

Returns `503` with `error: "ORDER_ADMISSION_UNAVAILABLE"` and `errorCode: "engine-busy"` when the venue declined to admit the cancellation. Nothing was cancelled and the identical request can be re-sent. See [Admission Errors](#admission-errors).

## Cancel Event Orders

```text
POST /api/v1/orders/cancel-event
```

**Authentication:** `API key HMAC`

Cancels all of your open orders across every market in one event.

Request body:

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `eventId` | UUID string | Yes | Parent event UUID. |

Response type: `CancelAllOrdersResponse`.

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `clearedMarkets` | UUID string[] | Yes | Event-market UUIDs with orders successfully cancelled. |
| `failedMarkets` | UUID string[] | No | Event-market UUIDs that failed cancellation. Retry these. |
| `notAdmitted` | `NotAdmittedMarket[]` | No | Event-markets the venue declined to admit. Present only when at least one market was refused this way. See [Admission Errors](#admission-errors). |

## Cancel All Orders

```text
POST /api/v1/orders/cancel-all
```

**Authentication:** `API key HMAC`

Response type: `CancelAllOrdersResponse`.

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `clearedMarkets` | UUID string[] | Yes | Market UUIDs with orders successfully cancelled. |
| `failedMarkets` | UUID string[] | No | Market UUIDs that failed cancellation. Retry these. |
| `notAdmitted` | `NotAdmittedMarket[]` | No | Markets the venue declined to admit. Present only when at least one market was refused this way. See [Admission Errors](#admission-errors). |

## Get Order Book Events

```text
GET /api/v1/orders/events
```

**Authentication:** `API key HMAC`

Query parameters:

| Query | Type | Required | Description |
| --- | --- | --- | --- |
| `page` | integer | No | Page number. Default `1`. |
| `limit` | integer | No | Results per page. Default `30`, maximum `100`. A larger value is reduced to `100`. |
| `total` | boolean | No | Include total count. Default `true`. |

Response type: `Pagination<OrderbookEvent>`.

When `total=true`, the count is bounded and the response can include `totalCapped`.

## Admission Errors

`POST /api/v1/orders` answers `503` when the order could not be signed, submitted or admitted. Every `503` on that endpoint means nothing was created, so re-sending the identical body cannot duplicate the order. Each response carries a `Retry-After` header and repeats the same figure in the body as `retryAfterSeconds`.

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `statusCode` | integer | Yes | `503`. |
| `error` | enum string | Yes | `SIGNING_UNAVAILABLE` or `ORDER_ADMISSION_UNAVAILABLE`. This is the discriminator; read it before `errorCode`. |
| `errorCode` | enum string | No | Present only when `error` is `ORDER_ADMISSION_UNAVAILABLE`. |
| `message` | string | Yes | Error message. |
| `retryAfterSeconds` | integer | Yes | Seconds to wait before retrying. Repeats the `Retry-After` header. |

`SIGNING_UNAVAILABLE` carries no `errorCode`. It has four causes: the signing provider being unavailable, rate-limiting Hit or having no slot in Hit's signing-admission queue; the request budget being exhausted before the order could be submitted; the duplicate guard's store being unavailable; and, on `POST /api/v1/orders/batch` only, the locking infrastructure being unavailable. Two of those four — the request budget and the duplicate guard's store — reach orders you signed yourself, so this status is not limited to orders Hit signs for you.

`ORDER_ADMISSION_UNAVAILABLE` carries an `errorCode`:

| `errorCode` | Meaning |
| --- | --- |
| `projection-stale` | The balance projection is not committing. |
| `balance-unknown` | Your balance has not been read from chain yet. |
| `balance-unprojected` | You have no projection row. |
| `collateral-source-unavailable` | The projection could not be queried at all. |
| `engine-busy` | The venue declined to admit the command at all. |
| `idempotency-unverifiable` | The venue could not check whether the idempotency key derived from your order had already been used. |

The first four mean your collateral could not be read. `engine-busy` is a stronger statement than the others: the command was not decided, not recorded and not partially applied, because the venue was at one of its own internal bounds. `idempotency-unverifiable` means the duplicate check could not be answered, so the venue refused rather than risk a second live order for one key.

For `engine-busy` and `idempotency-unverifiable`, re-send the identical body and do not change `salt`. The idempotency key is derived from the order's contents, so a new salt is a different order and defeats the deduplication.

None of these values means your order is invalid or your balance is insufficient. A genuinely insufficient balance is a `400`, and retrying it will not help.

`NotAdmittedMarket`, returned in `notAdmitted` by `POST /api/v1/orders/cancel-all` and `POST /api/v1/orders/cancel-event`:

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `market` | UUID string | Yes | Market UUID. |
| `errorCode` | enum string | Yes | Reason the venue declined the market, such as `engine-busy`. |
| `message` | string | Yes | Error message. |
| `retryAfterSeconds` | integer | Yes | Seconds to wait before retrying. |

A market listed in `notAdmitted` also appears in `failedMarkets`, so a client that ignores `notAdmitted` behaves exactly as it did before the field existed. Nothing in such a market was cancelled, and other markets in the same request may have been, so re-send rather than assuming either way.

## Order

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | UUID string | Yes | Order UUID. |
| `salt` | string | Yes | Order salt. |
| `maker` | address string | Yes | Maker address. |
| `signer` | address string | Yes | Signer address. |
| `taker` | address string | Yes | Taker address. |
| `tokenId` | string | Yes | Outcome token ID. |
| `makerAmount` | number | Yes | Maker amount in base units. |
| `takerAmount` | number | Yes | Taker amount in base units. |
| `expiration` | integer | Yes | Unix timestamp in seconds, or `0`. |
| `nonce` | integer | Yes | Maker nonce. |
| `feeRateBps` | integer | Yes | Fee rate in basis points. |
| `side` | integer | Yes | `0` buy, `1` sell. |
| `signatureType` | integer | Yes | Signature type. |
| `signature` | string | Yes | EIP-712 signature. |
| `market` | UUID string | Yes | Market UUID. |
| `orderType` | enum string | Yes | `GTC`, `FOK` or `FAK`. |
| `postOnly` | boolean | No | Whether the order was submitted as maker-only. |
| `owner` | UUID string | Yes | Hit user ID that owns the order. |
| `outcome` | string | Yes | Outcome label. |
| `status` | enum string | Yes | `PENDING`, `EXECUTING`, `EXECUTED`, `CANCELLED` or `REJECTED`. |
| `createdAt` | string (date-time) | Yes | Creation timestamp. |
| `amount` | number | Yes | Order size in display units. |
| `price` | number | Yes | Limit price. |
| `matchedAmount` | number | Yes | Filled amount in display units. |
| `hash` | string | Yes | EIP-712 order hash, lowercase `0x`-prefixed, 66 characters. If you sign client-side you can compute it before submission from the signed order fields and the exchange's EIP-712 domain; server-signed orders learn it from this response. The same value appears on every WebSocket event that names the order. |
| `priceUint` | string | Yes | Integer price representation. |
| `metadata` | object | No | Additional order metadata. |

`OrderWithMarket` contains every `Order` field plus required `marketInfo: OrderMarketInfo`. `GET /orders`, `GET /orders/paginated` and `GET /orders/{id}` return this enriched shape. Order values returned by create-order, cancel-order and cancel-market endpoints use the plain `Order` shape. Batch and cancel-all responses use the wrapper types documented above.

`OrderMarketInfo`:

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `icon` | string | Yes | Market icon URL. |
| `slug` | string | Yes | Market slug. |
| `title` | string | Yes | Market title. |
| `outcomeIndex` | integer | Yes | Outcome index associated with the order. |
| `eventId` | UUID string | No | Parent event UUID. |
| `eventName` | string | No | Parent event name. |
| `eventIcon` | string | No | Parent event icon URL. |
| `isMultiMarket` | boolean | Yes | Whether the parent event contains multiple markets. |
| `isSeriesLayout` | boolean | Yes | Whether the parent event uses the daily-series layout. |

## OrderbookEvent

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | UUID string | No | Event UUID. |
| `name` | string | No | Event name. |
| `slug` | string | No | Event slug. |
| `createdAt` | string (date-time) | No | Creation timestamp. |
| `startDate` | string (date-time) | No | Event start timestamp. |
| `endDate` | string (date-time) | No | Event end timestamp. |
| `single` | boolean | No | Whether the event has one market. |
| `marketsView` | `OrderbookEventMarket[]` | No | Active orderbook markets for the event. |

`OrderbookEventMarket`:

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | UUID string | No | Market UUID. |
| `name` | string | Yes | Market name. |
| `tokenId1` | string | Yes | Outcome 1 token ID. |
| `tokenId2` | string | Yes | Outcome 2 token ID. |
| `outcome1` | string | Yes | Outcome 1 label. |
| `outcome2` | string | Yes | Outcome 2 label. |
| `orderFeeRateBps` | integer | Yes | Active order fee rate in basis points. |
| `passiveOrderFeeRateBps` | integer | Yes | Passive order fee rate in basis points. |
